# Zebra 1.0 freeze checklist

*Drafted 2026-06-24. The concrete, checkable "do these, then flip the switch"
list. Pairs with `ROADMAP_TO_1.0.md` (strategy) and `API_FREEZE_AUDIT.md`
(the stdlib regret analysis). Items marked 🔲 need doing/deciding; ✅ are already
true; 👤 need Sean specifically.*

## A. Stdlib API consistency sweep (from `API_FREEZE_AUDIT.md`)

The freeze is "add but never remove," so these get locked permanently. Tier 1 is
the recommended pre-freeze fix list:

- ✅ **A1 — unify string-sequence returns on `List(str)`.** DONE 2026-06-24.
  `Net.resolve`, `re.findAll`, `re.groups` now return `List(str)` (preamble fns
  return `std.ArrayList([]const u8)`; both compilers route their for-loops through
  `.items`; selfhost gained `isStrListIter`, bootstrap gained a genForIn dispatch
  arm). Verified on both compilers (regex_groups/regex/dns run identically), gate
  byte-identical, smoke 173/173. Side-fix: two `std.ArrayListUnmanaged(u8) = .{}`
  preamble initializers that Zig 0.16 rejects (`→ .empty`); these were invisible
  because the smoke suite only emits, never compiles (see §D note).
- ✅ **A2 — `File.modtime → int?` DONE (2026-06-24).** Sean chose `int?` (clean
  optional, getenv pattern) over throws once it emerged that Zebra has no clean
  stdlib-throws mechanism (`File.read` actually `catch @panic`s). modtime now returns
  the mtime ms or `nil` when missing — both compilers, gate clean, smoke 174/174
  (new `modtime_optional_test`). The Mime/Csv items were audit errors (dropped).
  **Bonus:** the work uncovered + fixed a Zig-0.16 breakage — `statFile` gained an
  `options` param and `Stat.mtime` became `Io.Timestamp` — so `File.size`/`isFile`/
  `modtime` were all silently broken under 0.16 (invisible to the emit-only smoke);
  now fixed via `statFile(_io, p, .{})` + `mtime.toMilliseconds()`.
  **Remaining (deferred, Sean's call):** `File.read` panics and `File.size` still
  returns `-1` — a consistent File-error sweep (read/size → optional or throws) is
  post-modtime cleanup, not a blocker.
- ✅ **A3 — `Random` instance form** DONE (2026-06-28, #216). `Random.new(seed)`
  returns an independent, seedable PRNG instance with `rng.nextInt(lo,hi)` /
  `nextFloat()` / `nextBool()` / `bytes(n)`. Sean chose the `next*` naming
  (Roblox `:NextInteger` idiom; `int`/`float`/`bool` are keywords so the audit's
  literal names weren't usable). The thread-local statics (e1f1e1c) remain as the
  convenience surface. Backed by a pointer-wrapped `_Random` struct so
  `const rng = Random.new(..)` is legal. Verified: `test/random_instance_test.zbr`
  (determinism + range bounds), round-trip byte-identical.
- 👤 **B1–B4 — decisions to record** (parser optional-vs-throw consistency;
  whether connection `?` should ever carry a failure reason; inclusive-`randInt`
  and fetch-add semantics documented; `Dir.list` alias). See audit Tier 2.

## B. Pre-flight correctness items

- 🔲 **`_initIo` propagation gap** — selfhost-emitted dep modules get a
  non-propagating `_initIo`. Harmless today (no transitive dep does `_io` I/O).
  Decide: fix (mirror `src/CodeGen.zig` genModule's propagating form in
  `generateModuleWith`) or sign off in writing that it's acceptable for 1.0.
- ✅ **No hard bug blockers.** Open-bug triage (below) shows nothing that should
  block a stability freeze.

## C. Open-bug triage for 1.0

| Bug | Severity | Disposition for 1.0 |
|---|---|---|
| BUG-142 (too-few-args) | high → **mitigated** | UB is gone (`std.mem.zeroes`), and it warns. Full close (error + translator) is post-freeze, non-blocking. ✅ ship as-is |
| BUG-139 (default cue-init params not filled at omitting call sites) | low | Workaround: pass args explicitly. Not a blocker; fix post-1.0. |
| BUG-136 (`zebra file.zbr` captures child stdout vs streaming) | low | UX/capability; decision pending. Not a blocker. |
| BUG-014 (regex per-quantifier lazy) | medium | Explicitly **post-1.0** (architectural NFA change). Document the limitation. |
| BUG-026 (exposed-type method-chain return gaps) | low | Not manifesting; defer unless a concrete case appears. |
| All other BUG-* (111/112/113/115/125–135/137/138/140/141/099/…) | — | ✅ FIXED / NOT-A-BUG / swept. |

**Conclusion:** the tracker contains **no 1.0 blocker**. The open items are all
low-severity-with-workaround or consciously-deferred-post-1.0. This supports
freezing on quality grounds — *once the Tier-1 API sweep (§A) lands*, since those
are the only changes that are hard to make after the freeze.

## D. Real-world validation

- ✅ **Engine + real game logic runs** (2026-06-24): the GameEngine integration
  suite passes headlessly — health/regen/revive, event bus, weighted loot tables
  (genuine `monster_mayhem` mechanics) — confirming Zebra runs a substantial real
  program. (`docs/ROADMAP_TO_1.0.md` runtime frontier, validated.)
- 🔲 **Broaden the runtime pass** — wire more game scripts end-to-end; treat each
  runtime gap as a prioritized bug. (Front-end is 1482/1581 but only measures
  compiles; runtime is where remaining bugs live.) Not strictly a freeze blocker,
  but the strongest evidence for the 1.0 claim.
- 🔶 **Compile-check gate — ADDED 2026-06-25, 9 known failures remain.**
  `tools/compile_check.sh` now type-checks the emitted Zig for every positive-smoke
  test (`zig build-exe -fno-emit-bin`), closing the blind spot where the emit-only
  smoke + byte-identical gate never compiled user output. **This is the single most
  important pre-1.0 finding:** it surfaced that ~a sixth of the corpus emitted Zig
  that doesn't build (incomplete 0.16 migration + long-latent codegen bugs). Brought
  16 → 9; the remaining 9 are diagnosed in **`docs/COMPILE_CHECK_STATUS.md`** (3 share
  one interface-coercion root cause). **1.0 must not freeze until these are 0 and the
  gate is wired into `zig build`** — "compiles + runs", not "emits", is the real bar.
  (Dev default Zig is now **0.16.0** via `zvm use 0.16.0`.)

## E. Docs

- ✅ QUICKSTART current; CHANGELOG covers 0.1→1.0.
- 🔲 **Book reconciliation** (`zebra-language-book`) — written aspirationally;
  verify examples against the current compiler before it can back a 1.0 "learn
  Zebra" claim. Parallel doc track.

## F. The freeze act itself (👤 Sean)

- 👤 **Ratify the API surface.** Review `concept_zebra-language-warts` + the
  §A audit; confirm "happy with this surface forever (modulo additions)."
- 👤 **Publish the stability promise** (draft below).
- 👤 **Tag 1.0.**

---

## Draft stability promise (for Sean to edit/ratify)

> **Zebra 1.0 stability commitment.** From 1.0 onward, the public surface — the
> language syntax and semantics, and the signatures of the standard-library
> modules documented in QUICKSTART §31 — is **stable**. Within the 1.x series we
> may *add* (new modules, new methods, new optional/defaulted parameters, new
> syntax that doesn't change the meaning of existing code) but will not *remove*
> or *break* existing surface. Breaking changes are reserved for a future 2.0 and
> will be called out in the CHANGELOG with a migration note.
>
> Explicitly **not** covered by the promise: (a) generated-`.zig` output shape and
> internal compiler APIs; (b) the `zig"…"` escape hatch (its behavior tracks the
> underlying Zig version); (c) anything documented as "experimental" or "preview."
>
> Deprecation policy: a feature slated for removal at 2.0 is marked deprecated in
> a 1.x release (compiler warning) at least one minor version before removal.

---

*Recommended order: §A Tier-1 sweep → §B `_initIo` decision → §E book pass (can
run in parallel) → §F freeze. §D broadening is high-value but not gating.*
