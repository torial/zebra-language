.zig-cache/
zig-out/
*.exe
*.pdb
*.lib
*.dll

# Selfhost debug/temp artifacts
selfhost/*_err.txt
selfhost/codegen_roundtrip.zig

# Phase 16a+ typechecker port — .zig outputs are ephemeral until Phase 16c
# wires typechecker.zbr into the bootstrap FILES list.
selfhost/typechecker.zig
selfhost/typechecker_test.zig
