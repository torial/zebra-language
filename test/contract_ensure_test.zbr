class BankAccount
    var balance: int = 0

    def deposit(amount: int)
        ensure
            balance >= 0
        balance = balance + amount

class Main
    shared
        def main
            var acc = BankAccount()
            acc.deposit(100)
            print acc.balance
