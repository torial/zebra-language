# (was a `guard ... else` body until the keyword was freed 2026-09-15; same mismatch)
def makeInt(n: int): int
    if not n > 0
        return "guard else"
    return n

def main()
    print(makeInt(1).toString())
