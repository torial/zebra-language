class Greeter
    def greet(name: String): String
        return name

class Program
    static
        def main
            var g: Greeter = zig"Greeter{}"
            var msg: String = g.greet("world")
            print(msg)
