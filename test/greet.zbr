class Greeter
    def greet(name: str): str
        return name

class Program
    static
        def main
            var g: Greeter = zig"${Greeter}{}"
            var msg: str = g.greet("world")
            print(msg)
