class Counter
    static var count: int = 0

class Program
    static
        def main
            Counter.count = Counter.count + 1
            Counter.count = Counter.count + 1
            print(Counter.count)
