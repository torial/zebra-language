class Program
    shared
        def main
            var double = def(x: int) = x * 2
            var offset: int = 10
            var adder = def(x: int)
                capture
                    var offset: int = offset
                print x + offset
            print double(7)
            adder(5)
