use MathUtils
use StringHelper

class App
    shared
        def main
            var n: int = MathUtils.double(3)
            print n
            var g: str = StringHelper.greet("hello")
            print g
