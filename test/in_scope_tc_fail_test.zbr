# TC negative test: class missing begin() and end() used in `in` block.
# Expected: TC error mentioning "must define 'def begin()' and 'def end()'"

class Ungrouped
    var count: int = 0

def main()
    var g: Ungrouped = Ungrouped()
    in g
        g.count = 1
