class Math
    static
        def safeDiv(a: int, b: int): int throws
            if b == 0
                raise "division by zero"
            return a / b

def testSafeDiv()
    var quotient = Math.safeDiv(10, 2)?
    print quotient
catch |e|
    print "unexpected error: ".concat(e.message)

def testSafeDivFail()
    var result2 = Math.safeDiv(10, 0)?
    print result2
catch |e|
    print "caught: ".concat(e.message)

def main()
    # try prefix propagates errors on failure
    testSafeDiv()

    # division by zero — verify error is raised with correct message
    testSafeDivFail()

    print "ok"
