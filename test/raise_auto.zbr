class App
    static
        def risky(x: int): int
            if x < 0
                raise "negative value", x
            return x * 2

        def main
            var result: int = try App.risky(5) catch -1
            print result
