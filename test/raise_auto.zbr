class App
    shared
        def risky(x: int): int
            if x < 0
                raise "negative value", x
            return x * 2

        def main
            var result: int = 0
            try
                result = try App.risky(5)
            catch
                result = -1
            print result
