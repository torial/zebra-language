class Main
    static def main()
        # sys.readLine() with no stdin returns nil (EOF).
        var line = sys.readLine()
        if line == nil
            print "BUG-120 sys.readLine: OK"
        else
            print "BUG-120 sys.readLine: OK (with data)"
