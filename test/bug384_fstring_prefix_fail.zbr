# BUG-384: `f"n={x}"` -- a Python f-string -- lexed as the identifier `f` glued to a
# string and surfaced as "undefined name: 'f'". Every Zebra string interpolates, so the
# prefix is refused with the interpolation spelling.
def main()
    var x = 5
    print(f"n={x}")
