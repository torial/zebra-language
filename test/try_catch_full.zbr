class App
    static
        def risky(x: int): int throws
            if x < 0
                raise "negative input", x
            return x * 2

        def testRisky()
            var result: int = App.risky(5)?
            print result
            print "after try"
        catch
            print "caught an error"

        def main
            testRisky()
