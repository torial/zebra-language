class App
    static
        def risky(x: int): int throws
            if x < 0
                raise "negative input", x
            return x * 2

        def main
            try
                var result: int = try App.risky(5)
                print result
            catch
                print "caught an error"

            print "after try"
