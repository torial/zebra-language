# Corpus files whose status is legitimately NOT asserted by a smoke helper.
# Format:  <name>  # <reason>      -- a reason is REQUIRED (tools/registration_check.py).
#
# These are MODULES, not tests: they have no main() and are exercised through whatever
# imports them. Demanding a registration for each would be noise, and noise is how a
# gate gets ignored. Anything NOT here and not registered is real debt -- see BUG-243.

MathUtils  # helper module imported by other tests; has no main()
StringHelper  # helper module imported by other tests; has no main()
bug082_lib  # helper module imported by other tests; has no main()
bug124_boxed_ctor_lib  # helper module imported by bug124_boxed_nilable_ctor_test; has no main()
bug168_crossmod_prim_return_lib  # helper module imported by other tests; has no main()
bug221_transitive_init_leaf  # support module for a bug fixture; exercised through its importer
bug221_transitive_init_mid  # support module for a bug fixture; exercised through its importer
bug235_use_lib  # helper module imported by other tests; has no main()
bug238_except_lib  # helper module imported by other tests; has no main()
bug346_lib  # helper module imported by bug346_imported_call_method_test; has no main()
crossmod_expose_lib  # helper module imported by other tests; has no main()
crossmod_hatopt_lib  # helper module imported by other tests; has no main()
crossmod_modvar_lib  # helper module imported by other tests; has no main()
crossmod_optret_geom  # cross-module test support; exercised through its importer
crossmod_optret_lib  # helper module imported by other tests; has no main()
crossmod_types_lib  # helper module imported by other tests; has no main()
expose_lib  # helper module imported by other tests; has no main()
partial_shared_test.extra  # partial of test/partial_shared_test.zbr — auto-loaded by the compiler, not a test
partial_test.math  # partial of test/partial_test.zbr — auto-loaded by the compiler, not a test
partial_test.meta  # partial of test/partial_test.zbr — auto-loaded by the compiler, not a test
tc_check  # module of the tc_* typechecker experiment; imported, not run directly
tc_infer  # module of the tc_* typechecker experiment; imported, not run directly
tc_scope  # module of the tc_* typechecker experiment; imported, not run directly
tc_smoke_types  # module of the tc_* typechecker experiment; imported, not run directly
tc_stdlib  # module of the tc_* typechecker experiment; imported, not run directly
tc_types  # module of the tc_* typechecker experiment; imported, not run directly
typechecker  # module of the tc_* typechecker experiment; imported, not run directly
val_lib  # helper module imported by other tests; has no main()
witness_zig_backend_literal  # asymmetry witness — asserted by tools/check_mode_check.sh, which REQUIRES it to pass -c and fail --check-full; a smoke registration would contradict that
bug293_xmod_container_lib  # helper module imported by bug293_xmod_container_test; has no main()
bug295_cycle_lib  # the other half of BUG-295's import cycle; exercised through bug295_cycle_test
bug295_diamond_lib_leaf  # helper module imported by other tests; has no main()
bug295_diamond_lib_left  # helper module imported by other tests; has no main()
bug295_diamond_lib_right  # helper module imported by other tests; has no main()
bug295_shared_state_leaf  # helper module imported by other tests; has no main()
bug295_shared_state_left  # helper module imported by other tests; has no main()
bug295_shared_state_right  # helper module imported by other tests; has no main()
tc_merge_fixture  # NOT a test and cannot compile standalone BY CONSTRUCTION: it holds git conflict markers (<<<<<<< / =======) and is INPUT to `zebra typecheck-merge`. The EMITFAIL is `unexpected top-level token: '<<'`, which is the file doing its job. Do not "fix" it.
prog_args_passthrough_test  # asserted INLINE in selfhost_smoke.sh (BUG-347: it needs argv after `--`, which no smoke_* helper passes); the registration scanner cannot see the inline form
