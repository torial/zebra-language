# clean (BUG-176 fixed): inferred `var cols = s.split(sep)` materializes into a
# List(str), so cols[0] is indexable. (Was SHARED-GAP: a lazy SplitIterator.)
def main()
    var line = "a,b,c"
    var cols = line.split(",")
    print("first=${cols[0]}")
