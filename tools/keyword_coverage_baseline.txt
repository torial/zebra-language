# Keywords no tracked test/ or examples/ file uses.
# SHRINK THIS LIST; never grow it. Each line is a language feature
# shipped without a program that exercises it. The two entries this
# gate was written over -- `protected` and `internal` -- were BOTH
# defective (BUG-315, BUG-316).
#
# STATUS, 2026-08-29 -- READ BEFORE ACTING ON ANY OF THESE.
#
#   abstract  PARKED behind the bootstrap sunset. Sean marked it for removal; it is
#             BOOTSTRAP-ONLY -- `class X is abstract` compiles under zebra-bootstrap and is
#             REJECTED by the shipping selfhost. Removing it means ~62 lines across grammar
#             rules, AST node types and codegen. If the bootstrap retires it vanishes for
#             free. See NEXT_STEPS "FREEZE THE BOOTSTRAP, THEN MOVE TO N-1".
#   defer     PARKED, same reason. Bootstrap-only, and it is FULLY implemented there --
#   errdefer  StmtDefer is a real AST node with `genDefer` at src/CodeGen.zig:13425.
#             Sean: "bleedthrough from zig and we can kill".
#   vari      Marked for removal (variadic parameters, a feature not being supported).
#             Rejected by BOTH compilers today, so this one is not bootstrap-parked -- it is
#             simply unimplemented in both and can go whenever someone is in Token.zig.
#   implies   KEPT, deliberately. A reserved word for a contract operator (`a implies b`,
#             Eiffel-style logical implication) that is NOT implemented -- lint_reserved_words
#             classifies it R1 UNREACHABLE and baselines it, and src/Parser.zig:600 has a test
#             pinning it reserved. The asymmetry is the argument: reserving a rare identifier
#             costs little now, while ADDING a keyword back later is a breaking change, so
#             holding the option is worth more than freeing the word.
#
# `continue` left this list on 2026-08-29 (test/loop_continue_break_test.zbr).
#
abstract
defer
errdefer
implies
vari
